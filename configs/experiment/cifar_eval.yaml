# @package _global_

defaults:
  - override /datamodule: cifar10.yaml
  - override /model: timm.yaml
  - override /callbacks: default.yaml
  - override /trainer: default.yaml

tags: ["cifar10"]

#path needs to be pasted here to be overriden in ckpt_path in eval.yaml
ckpt_path: ???